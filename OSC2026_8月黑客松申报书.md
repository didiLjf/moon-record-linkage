# 🏆 开源创新大赛 2026 (OSC2026) 8月黑客松项目申报书

---

## 📋 一、 项目基本信息

| 申报项 | 详细信息 |
| :--- | :--- |
| **项目名称** | `moon-record-linkage` (MoonBit 原生实体匹配与记录消歧引擎) |
| **参赛赛道** | MoonBit 语言生态专项赛道（OSC2026 8月黑客松） |
| **参赛选手 / 团队** | didiLjf (GitLink 账号: DidiLs) |
| **项目开源协议** | Apache License 2.0 |
| **源码规模** | 6,205 行纯 MoonBit 原生代码（符合 4,000 ~ 10,000 行高标准要求） |
| **单元测试覆盖** | 97 / 97 个单元测试用例（测试通过率 100%） |
| **开源仓库地址** | [GitHub 仓库](https://github.com/didiLjf/moon-record-linkage) \| [GitLink 仓库](https://gitlink.org.cn/DidiLs/moon-record-linkage) |

---

## 💡 二、 项目立项背景与核心痛点

### 1. 行业数据治理痛点
在现代大数据治理、主数据管理（MDM）、跨部门数据共享及多源数据库融合场景中，**实体消歧（Entity Resolution）与记录匹配（Record Linkage / Deduplication）** 是极其核心的关键技术。传统系统在面对拼写错误、格式不一致、缺失值及同名异物/异名同物等脏数据时，难以兼顾精度与运行效率。

### 2. MoonBit 生态空白与机遇
MoonBit 作为面向 WebAssembly 和 Native 的新一代云原生与边缘计算编程语言，拥有极高的运行效率和类型安全保障。然而，目前 MoonBit 生态中**严重缺乏支持海量异构数据去重、概率图匹配与文本相似度计算的成熟基础设施**。`moon-record-linkage` 项目应运而生，旨在打造 MoonBit 生态中最完善、高扩展、工业级的实体匹配引擎。

---

## 🛠️ 三、 核心技术架构与算法设计

本项目采用模块化分层架构，全栈基于 MoonBit 纯原生实现，零外部 C/JS 依赖：

```text
               ┌─────────────────────────────────────────┐
               │    多源异构记录输入 (Records Input)      │
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [1] 文本标准化与清洗 (Normalization)     │
               │  - 大小写折叠 / 空白压缩 / 标点剥离       │
               │  - 域清洗规则 (公司名/电话/身份证/地址)    │
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [2] 阻断算法 (Blocking & Reduction)     │
               │  - Standard Blocking / SNM 滑动窗口     │
               │  - Canopy 聚类 / LSH MinHash 局部敏感哈希│
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [3] 多维度距离度量 (Similarity Metrics)  │
               │  - 编辑距离: Levenshtein, Damerau, LCS  │
               │  - 相似度: Jaro-Winkler, N-Gram Jaccard │
               │  - 语音编码: Soundex, Metaphone, NYSIIS │
               │  - 空间地理: Haversine 地球大圆距离      │
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [4] 混合打分与模型推理 (Scoring & Model) │
               │  - Fellegi-Sunter 概率模型 (EM 自适应)   │
               │  - 复合规则引擎 (Rule Engine AND/OR/NOT)  │
               │  - 权重组合打分器 (Hybrid Scorer)       │
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [5] 实体图聚类与规范化 (Graph & Merger)  │
               │  - DSU 并查集 (路径压缩 + 按秩合并)      │
               │  - 规范记录生成 (Majority Vote / Recent) │
               └────────────────────┬────────────────────┘
                                    │
               ┌────────────────────▼────────────────────┐
               │ [6] 结果导出与评估 (Export & Report)    │
               │  - JSON / CSV 导出, P/R/F1 性能评估报告  │
               └─────────────────────────────────────────┘
```

---

## 🌟 四、 项目核心创新点与竞争优势

1. **高效 Block 阻断技术（降维打击 \(O(N^2)\) 复杂度）**
   支持 Standard Blocking、Sorted Neighborhood Method (SNM)、Canopy Clustering 与 LSH (MinHash) 局部敏感哈希，能够将大规模记录对齐的比较规模缩减 **99% 以上**。
2. **Fellegi-Sunter 概率模型与 EM 无监督训练**
   内置经典的 Fellegi-Sunter 概率匹配框架，支持通过 EM（期望最大化）算法无监督估计匹配概率 $m$ 与 $u$，自动调整字段匹配权重。
3. **并查集（DSU）实体图聚类与多策略融合**
   基于高效 DSU 实现实体联通分量快速划归，并提供 `MajorityVote`（多数表决）、`HighestConfidence`（最高置信度）和 `MostRecent`（最新时间戳）等多策略规范记录（Canonical Record）自动融合。
4. **全套语音与空间算法支持**
   专门针对姓名、电话、地理位置等异构属性，实现了 Soundex、Metaphone、NYSIIS、MRA 语音编码及 Haversine 地理距离衰减算法。

---

**申报人 / 团队**：didiLjf  
**申报日期**：2026 年 8 月
